package banco;

public class Banco extends Cuenta{
    //saldo -> priavte -> no tiene acceso
    //codigo -> default -> tiene acceso
    //oficina -> protected -> tiene acceso
    //titular -> public -> tiene acceso
}
